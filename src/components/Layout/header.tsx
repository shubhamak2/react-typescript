import React from 'react';

import * as styles from '../../styles';

const Header = () => {
  return (
    <div className={styles.header}>
      <h1>Github Users</h1>
    </div>
  );
};

export default Header;
